// importing the Pizza and Comment models so it's accessible from one file
const Pizza = require('./Pizza');
const Comment = require('./Comment');

// exporting the Pizza and Comment models so it's accessible from one file
module.exports = { Pizza, Comment };
